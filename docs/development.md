# Development

The public repository contains only source required to build and deploy ClassMoney 2.0.

Local secrets and environment-specific configuration must remain outside Git.

Database migrations live under `migrations/`.
